# ===== AI 忽略清单 =====
# 本工作区 AI 编码工具共用的文件忽略清单（唯一一份，勿再派生副本），语法与 .gitignore 相同。
# 原生支持 gitignore 风格忽略文件的工具（Cursor .cursorignore / Windsurf .windsurfignore /
# Gemini CLI .geminiignore 等）若未自动识别本文件名，可各自指认或链接到本文件。
# Claude Code 不读取本文件——如需硬性拦截，在 .claude/settings.local.json（个人配置，不入库）
# 手工添加 permissions.deny 的 Read(...) 规则。
# 与 .gitignore 的关系：两份清单职责不同，.gitignore 管 git，本清单管 AI 读取，各自独立维护。

# ---- 依赖与构建产物（大体积，读源码用不到）----
**/node_modules/
RuoYi-Vue3/dist/
**/target/
RuoYi-Vue-GO/docs/

# ---- Python 运行时产物 ----
**/__pycache__/
**/.pytest_cache/
**/.ruff_cache/
**/.venv/
**/venv/

# ---- 日志 / 上传下载 / 数据卷（运行时数据，不属于代码）----
**/logs/
**/*.log
**/upload_path/
**/download_path/
docker/data/

# ---- IDE 配置 ----
.idea/
.vscode/

# ---- 含真实凭据的配置（与 .gitignore 同口径：绝不提交，AI 也不读）----
**/.env.prod
**/.env.test
docker/.env

# 刻意不屏蔽（勿添加）：
#   .env.dev —— 三版后端配置同步依赖它，且仅含本地 Docker 默认凭据（见根 .gitignore 注释）
#   AGENTS.local.md —— 刻意入库的本机配置参考
#   github-io-homepage.md —— 本地话术稿，由 AI 协助维护，需可读
